import { Link } from "react-router-dom"

export const TasksPage = () => {
    return (
        <div>
            <h1>Tasks Page</h1>
            <Link to="/login">Go to Login</Link>
            <Link to="/login">Go to Login</Link>
        </div>
    )
}